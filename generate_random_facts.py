# Randfacts is a python library that generates random facts.
# You can use randfacts.getFact() to return a 
# random fun fact. Use the below command to install the module
# in the system pip3 install randfacts

import randfacts

facts = randfacts.getFact(True)

print(facts)